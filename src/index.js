/**
 * Supported water API. Atmospheric scattering is available independently from `water/sky`.
 * Scene presets, grading, camera direction and geometry belong to the consuming application.
 */

export { Water, WATER_DEFAULTS, bakeShoreMap } from './water/index.js';
